<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:App15"
             x:Class="App15.MainPage">
<StackLayout>
  <Label Text="Welcome to Xamarin Forms!"
           VerticalOptions="Center"
           HorizontalOptions="Center" />
  <Button Text="Ho" Clicked="onClickedButtin" />
  <ListView x:Name="FlightView" ItemsSource="{Binding Path=oflight}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell >
          <StackLayout Orientation="Vertical"
                       HorizontalOptions="FillAndExpand"
                       BackgroundColor="#eee">
            <StackLayout Orientation="Horizontal">
              <Label Text="{Binding Path=CIata}"
              TextColor="#f35e20" />
              <Label Text="{Binding Path=FlightNumber}"
              TextColor="#f35e20" />
              <Label Text="{Binding Path=FlightSTA, StringFormat='Date: {0:dd-MM-yyyy}'}"
              HorizontalOptions="EndAndExpand"
              TextColor="#503026" />
            </StackLayout>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</StackLayout>
</ContentPage>